import "../assets/styles/NotFound.css"

function NotFound() {
    return (
        <div>
            <h1>Page not found...</h1>
            <h3>Please make sure the URL is typed correctly, and try again</h3>
        </div>
    )
}

export default NotFound